name: kick
arch: amd64
platform: linux
version: 'v${VERSION}'
section: default
maintainer: ${AUTHOR} <${EMAIL}>
description: |
    Create your projects using templates under git version control.
    kick will automatically download any git repository templates defined in
    your configuration file or download a git repository from any upstream
    template provider.
    Project Templates can also be referenced on local disk
license: Apache-2.0
files:
    dist/kick_linux_amd64/kick: /usr/bin/kick

rpm:
    group: Unspecified
    compression: lzma
